

/**
 * this is a helper to create animated tiles, 
 * more specifically an object of this interface will be inside animated Tiles array
 * 
 */
export interface AnimatedTileConfig
{
    index:number;
    lastIndex:number;
    srcY:number;
}